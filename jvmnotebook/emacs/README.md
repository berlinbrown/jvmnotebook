# Emacs configuration examples

This folder contains an `emacs_conf/` folder with example Emacs configuration files and scripts for starting Emacs on Windows and other platforms. These are reference files showing how the author configured Emacs for working with the demos.