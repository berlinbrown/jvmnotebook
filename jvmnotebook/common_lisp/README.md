# Common Lisp examples

This folder contains Common Lisp examples and a collection of `euler` solutions written in Common Lisp (files like `euler1.lisp`, `euler10.lisp`, etc.).

There is a `Makefile` for running some of the examples. These are small, educational programs focused on algorithmic problems and using a Lisp implementation on the JVM or external Lisp tools.